services:
  restaurant-db:
    ports:
      - "5232:5432"
    env_file:
      - .env.dev

  app:
    build:
      target: dev
    env_file:
      - .env.dev
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload